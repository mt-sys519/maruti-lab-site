"use client";
/* eslint-disable no-irregular-whitespace -- the wide spaces are ideographic
   spaces inside YURAMEKI's own Japanese copy, set deliberately. */

import { ChangeEvent, PointerEvent as ReactPointerEvent, useCallback, useEffect, useRef, useState } from "react";
import { GIFEncoder, applyPalette, quantize } from "gifenc";
import { BufferTarget, CanvasSource, Mp4OutputFormat, Output, QUALITY_HIGH, canEncodeVideo } from "mediabunny";
import UPNG from "upng-js";
import { supportUrl } from "./site-links";

type Vec = { x:number; y:number };
type Point = { id: number; x: number; y: number; dx: number; dy: number; vx: number; vy: number; amplitude: number; stiffness: number; damping: number; radius: number; feather: number; outer: number; whole: number; speed?: number; synced?: boolean; phaseOffset?: number; curve?: boolean; polygon?: Vec[]; lightAspect?: number; lightAngle?: number; impact?: number };
type ExportKind = "gif" | "apng" | "webp" | "mp4";
type MotionKind = "breath" | "flutter" | "light" | "ripple";
type ModeEditorState = { points: Point[]; selected: number | null; draft: Vec[]; draftCurve: boolean; motionSpeed: number; rippleInterval: number };
type ReadyExport = { url: string; blob: Blob; name: string; label: string; revision: number; mode: MotionKind };
type ExportCrop = { x: number; y: number; width: number; height: number };
type SaveHandle = { createWritable(): Promise<{ write(data: Blob): Promise<void>; close(): Promise<void> }> };
type PickerWindow = Window & { showSaveFilePicker?: (options: { suggestedName: string; types: { description: string; accept: Record<string,string[]> }[] }) => Promise<SaveHandle> };
type NativeShareData = { files?: File[]; title?: string; text?: string; url?: string };
type ShareNavigator = Navigator & { canShare?: (data: NativeShareData) => boolean; share?: (data: NativeShareData) => Promise<void> };
const W = 720, H = 720, GRID = 28;
const INITIAL_POINT: Point = { id:1,x:475,y:335,dx:0,dy:0,vx:0,vy:0,amplitude:4,stiffness:48,damping:62,radius:108,feather:12,outer:1,whole:0,speed:50,synced:true,phaseOffset:0,curve:true,polygon:[{x:354,y:348},{x:384,y:274},{x:454,y:254},{x:529,y:280},{x:548,y:407},{x:410,y:414}] };
const CAT_POINT: Point = { id:2,x:130,y:560,dx:0,dy:0,vx:0,vy:0,amplitude:4,stiffness:48,damping:66,radius:68,feather:10,outer:1,whole:0,speed:50,synced:false,phaseOffset:2.2,curve:true,polygon:[{x:126,y:516},{x:170,y:505},{x:202,y:545},{x:190,y:607},{x:145,y:630},{x:98,y:590}] };
const INITIAL_POINTS=[INITIAL_POINT,CAT_POINT];
const BLUE_KOINOBORI_POINT: Point = { id:1,x:500,y:250,dx:0,dy:0,vx:0,vy:0,amplitude:6,stiffness:36,damping:66,radius:132,feather:9,outer:1,whole:0,speed:50,synced:true,phaseOffset:0,curve:true,polygon:[{x:276,y:178},{x:413,y:178},{x:517,y:184},{x:603,y:184},{x:689,y:195},{x:649,y:241},{x:689,y:299},{x:603,y:316},{x:517,y:322},{x:413,y:310},{x:310,y:287},{x:276,y:235}] };
const RED_KOINOBORI_POINT: Point = { id:2,x:430,y:515,dx:0,dy:0,vx:0,vy:0,amplitude:6,stiffness:40,damping:70,radius:112,feather:9,outer:1,whole:0,speed:50,synced:false,phaseOffset:1.35,curve:true,polygon:[{x:258,y:454},{x:356,y:454},{x:459,y:465},{x:557,y:471},{x:591,y:488},{x:551,y:514},{x:580,y:554},{x:517,y:571},{x:436,y:577},{x:356,y:563},{x:287,y:540},{x:258,y:494}] };
const FLUTTER_POINTS=[BLUE_KOINOBORI_POINT,RED_KOINOBORI_POINT];
const LIGHT_POINT:Point={id:1,x:300,y:485,dx:0,dy:0,vx:0,vy:0,amplitude:24,stiffness:72,damping:68,radius:170,feather:28,outer:0,whole:8,speed:50,synced:true,phaseOffset:0,lightAspect:1.8,lightAngle:-.35};
const LIGHT_POINTS=[LIGHT_POINT];
const RIPPLE_POINT:Point={id:1,x:418,y:355,dx:0,dy:0,vx:0,vy:0,amplitude:2.6,stiffness:42,damping:72,radius:118,feather:26,outer:0,whole:8,speed:50,synced:true,phaseOffset:0,impact:25};
const RIPPLE_POINTS=[RIPPLE_POINT];
const DEMO_IMAGES:Record<MotionKind,string>={breath:"/yurameki/demo-breath.webp",flutter:"/yurameki/demo-flutter.webp",light:"/yurameki/demo-light.webp",ripple:"/yurameki/demo-ripple.webp"};
const DEMO_STATUSES:Record<MotionKind,string>={breath:"花魁と黒猫に静かな呼吸を宿しています。範囲1・2を切り替えて試せます",flutter:"2匹の鯉へ風を渡しています。範囲1・2を切り替えて試せます",light:"蝋燭の灯りが静かにまたたいています。点を動かして光の位置を試せます",ripple:"水面へ静かな輪をひろげています。中心と届く範囲を動かして試せます"};

function presetsForMode(kind:MotionKind){return kind==="flutter"?FLUTTER_POINTS:kind==="light"?LIGHT_POINTS:kind==="ripple"?RIPPLE_POINTS:INITIAL_POINTS}
function clonePoints(values:Point[]){return values.map(point=>({...point,polygon:point.polygon?.map(value=>({...value}))}))}
function settledPoints(values:Point[]){return clonePoints(values).map(point=>({...point,dx:0,dy:0,vx:0,vy:0}))}
function initialModeState(kind:MotionKind):ModeEditorState{const points=clonePoints(presetsForMode(kind));return{points,selected:points[0]?.id??null,draft:[],draftCurve:true,motionSpeed:50,rippleInterval:3.4}}
function motionPhase(phase:number,point:Point,masterSpeed:number,speedOverride?:number){const speed=speedOverride??(point.synced===false?(point.speed??masterSpeed):masterSpeed),offset=point.synced===false?(point.phaseOffset??0):0;return phase*speed/50+offset}

function lightGeometry(point:Point){const aspect=Math.max(.35,Math.min(2.5,point.lightAspect??1)),angle=point.lightAngle??0,cx=point.x+point.dx,cy=point.y+point.dy,reach=point.radius*aspect,ax=cx+Math.cos(angle)*reach,ay=cy+Math.sin(angle)*reach,bx=cx-Math.cos(angle)*reach,by=cy-Math.sin(angle)*reach,overflow=(x:number,y:number)=>Math.max(0,18-x)+Math.max(0,x-(W-18))+Math.max(0,18-y)+Math.max(0,y-(H-18)),useOpposite=overflow(bx,by)<overflow(ax,ay);return{aspect,angle,cx,cy,handleX:useOpposite?bx:ax,handleY:useOpposite?by:ay}}
function rippleGeometry(point:Point){const cx=point.x+point.dx,cy=point.y+point.dy,radius=Math.max(28,point.radius),right={x:cx+radius,y:cy},left={x:cx-radius,y:cy},handle=right.x>W-18&&left.x>=18?left:right;return{cx,cy,radius,handleX:handle.x,handleY:handle.y}}
type RippleWaveState={progress:number;life:number};
type ExportLoopPlan={frameCount:number;durationMs:number;phaseMultiplier:number;speedOverrides:Map<number,number>};
function rippleWaveStates(clock:number,point:Point,interval:number,speed:number):RippleWaveState[]{
 const safeInterval=Math.max(.4,interval),travel=Math.max(.35,2.35*50/Math.max(1,speed)),offsetRatio=point.synced===false?(((point.phaseOffset??0)/(Math.PI*2))%1+1)%1:0,offset=offsetRatio*safeInterval,newestAge=((clock-offset)%safeInterval+safeInterval)%safeInterval,waves:RippleWaveState[]=[];
 if(speed<=0)return waves;
 for(let age=newestAge;age<travel&&waves.length<24;age+=safeInterval){const progress=age/travel,life=Math.max(0,Math.min(1,progress/.07,(1-progress)/.13));if(life>0)waves.push({progress,life})}
 return waves
}

function exportLoopPlan(points:Point[],masterSpeed:number):ExportLoopPlan{
 const naturalSpeeds=points.map(point=>Math.max(0,point.synced===false?(point.speed??masterSpeed):masterSpeed)),reference=Math.max(1,masterSpeed>0?masterSpeed:naturalSpeeds.find(speed=>speed>0)??50),referencePeriod=75000/reference,maxCycles=Math.max(1,Math.min(6,Math.floor(12000/referencePeriod)));let chosenCycles=1,bestError=Infinity;
 for(let cycles=1;cycles<=maxCycles;cycles++){let error=0;for(const speed of naturalSpeeds){if(speed<=0)continue;const closedCycles=Math.max(1,Math.round(cycles*speed/reference)),closedSpeed=closedCycles*reference/cycles;error=Math.max(error,Math.abs(closedSpeed-speed)/speed)}if(error<bestError-.0001){bestError=error;chosenCycles=cycles}if(error<.005)break}
 const speedOverrides=new Map<number,number>();points.forEach((point,index)=>{const speed=naturalSpeeds[index];if(speed<=0){speedOverrides.set(point.id,0);return}const closedCycles=Math.max(1,Math.round(chosenCycles*speed/reference));speedOverrides.set(point.id,closedCycles*reference/chosenCycles)});
 return{frameCount:Math.min(96,24*chosenCycles),durationMs:referencePeriod*chosenCycles,phaseMultiplier:chosenCycles*50/reference,speedOverrides}
}

function insidePolygon(x:number,y:number,polygon:Vec[]){let inside=false;for(let i=0,j=polygon.length-1;i<polygon.length;j=i++){const a=polygon[i],b=polygon[j];if((a.y>y)!==(b.y>y)&&x<(b.x-a.x)*(y-a.y)/(b.y-a.y)+a.x)inside=!inside}return inside}
function segmentDistance(x:number,y:number,a:Vec,b:Vec){const dx=b.x-a.x,dy=b.y-a.y,length=dx*dx+dy*dy||1,t=Math.max(0,Math.min(1,((x-a.x)*dx+(y-a.y)*dy)/length)),px=a.x+t*dx,py=a.y+t*dy;return Math.hypot(x-px,y-py)}
function polygonWeight(x:number,y:number,polygon:Vec[],feather:number,outer:number){const inside=insidePolygon(x,y,polygon);let distance=Infinity;for(let i=0;i<polygon.length;i++)distance=Math.min(distance,segmentDistance(x,y,polygon[i],polygon[(i+1)%polygon.length]));if(inside)return Math.min(1,.5+distance/(Math.max(1,feather)*2));if(outer<=0)return 0;return Math.max(0,.5*(1-distance/outer))}
function smoothPolygon(points:Vec[]){if(points.length<3)return points;const result:Vec[]=[];for(let i=0;i<points.length;i++){const p0=points[(i-1+points.length)%points.length],p1=points[i],p2=points[(i+1)%points.length],p3=points[(i+2)%points.length];for(let step=0;step<8;step++){const t=step/8,t2=t*t,t3=t2*t;result.push({x:.5*((2*p1.x)+(-p0.x+p2.x)*t+(2*p0.x-5*p1.x+4*p2.x-p3.x)*t2+(-p0.x+3*p1.x-3*p2.x+p3.x)*t3),y:.5*((2*p1.y)+(-p0.y+p2.y)*t+(2*p0.y-5*p1.y+4*p2.y-p3.y)*t2+(-p0.y+3*p1.y-3*p2.y+p3.y)*t3)})}}return result}
const smoothCache=new WeakMap<Vec[],Vec[]>();
function regionShape(point:Point){if(!point.polygon)return undefined;if(!point.curve)return point.polygon;const cached=smoothCache.get(point.polygon);if(cached)return cached;const shape=smoothPolygon(point.polygon);smoothCache.set(point.polygon,shape);return shape}
function datedName(extension:string){const d=new Date(),pad=(n:number)=>String(n).padStart(2,"0");return `yurameki-${d.getFullYear()}${pad(d.getMonth()+1)}${pad(d.getDate())}-${pad(d.getHours())}${pad(d.getMinutes())}${pad(d.getSeconds())}.${extension}`}
function breathDepthToSlider(value:number){return value<=10?value*6:60+40*Math.sqrt((value-10)/50)}
function sliderToBreathDepth(value:number){return value<=60?value/6:10+50*((value-60)/40)**2}


function write24(view: DataView, offset: number, value: number) {
 view.setUint8(offset, value & 255); view.setUint8(offset + 1, (value >> 8) & 255); view.setUint8(offset + 2, (value >> 16) & 255);
}
function webpChunk(tag: string, payload: Uint8Array) {
 const out=new Uint8Array(8+payload.length+(payload.length%2)); const view=new DataView(out.buffer);
 for(let i=0;i<4;i++)out[i]=tag.charCodeAt(i); view.setUint32(4,payload.length,true); out.set(payload,8); return out;
}
function joinBytes(parts: Uint8Array[]) { const size=parts.reduce((n,p)=>n+p.length,0),out=new Uint8Array(size);let at=0;for(const p of parts){out.set(p,at);at+=p.length}return out; }
async function canvasWebPBytes(canvas: HTMLCanvasElement) {
 const blob=await new Promise<Blob>((resolve,reject)=>canvas.toBlob(value=>value?resolve(value):reject(new Error("WebP is not supported")),"image/webp",.88));
 return new Uint8Array(await blob.arrayBuffer());
}
async function animatedWebP(frameCount: number, delay: number, width:number, height:number, render: (phase:number)=>HTMLCanvasElement, progress: (frame:number)=>void) {
 const frameParts:Uint8Array[][]=[];let hasAlpha=false;
 for(let i=0;i<frameCount;i++){const canvas=render(i/frameCount*Math.PI*2),bytes=await canvasWebPBytes(canvas),parts:Uint8Array[]=[];canvas.width=canvas.height=1;let offset=12;while(offset+8<=bytes.length){const tag=String.fromCharCode(...bytes.slice(offset,offset+4)),length=new DataView(bytes.buffer,bytes.byteOffset+offset+4,4).getUint32(0,true),end=offset+8+length+(length%2);if(tag==="ALPH"){hasAlpha=true;parts.push(bytes.slice(offset,end))}else if(tag==="VP8 "||tag==="VP8L")parts.push(bytes.slice(offset,end));offset=end}if(!parts.length)throw new Error("WebP frame could not be encoded");frameParts.push(parts);progress(i+1);if(i%4===3)await new Promise<void>(resolve=>requestAnimationFrame(()=>resolve()))}
 const vp8x=new Uint8Array(10),vp8xView=new DataView(vp8x.buffer);vp8x[0]=0x02|(hasAlpha?0x10:0);write24(vp8xView,4,width-1);write24(vp8xView,7,height-1);
 const anim=new Uint8Array(6);new DataView(anim.buffer).setUint16(4,0,true);
 const chunks=[webpChunk("VP8X",vp8x),webpChunk("ANIM",anim)];
 for(const parts of frameParts){const head=new Uint8Array(16),view=new DataView(head.buffer);write24(view,6,width-1);write24(view,9,height-1);write24(view,12,delay);head[15]=0x02;chunks.push(webpChunk("ANMF",joinBytes([head,...parts])))}
 const body=joinBytes(chunks),file=new Uint8Array(12+body.length),view=new DataView(file.buffer);file.set([82,73,70,70],0);view.setUint32(4,file.length-8,true);file.set([87,69,66,80],8);file.set(body,12);return new Blob([file.buffer],{type:"image/webp"});
}
function makeDemo(){const c=document.createElement("canvas");c.width=W;c.height=H;const g=c.getContext("2d")!;const paper=g.createRadialGradient(360,310,20,360,360,520);paper.addColorStop(0,"#fbf7ef");paper.addColorStop(1,"#e9dfcf");g.fillStyle=paper;g.fillRect(0,0,W,H);return c}
function fitImage(img:HTMLImageElement,cover:boolean,targetWidth=W,targetHeight=H){const c=document.createElement("canvas");c.width=targetWidth;c.height=targetHeight;const g=c.getContext("2d")!;g.fillStyle="#eef2eb";g.fillRect(0,0,targetWidth,targetHeight);const scale=(cover?Math.max:Math.min)(targetWidth/img.width,targetHeight/img.height),width=img.width*scale,height=img.height*scale;g.drawImage(img,(targetWidth-width)/2,(targetHeight-height)/2,width,height);return c}
function exportCrop(img:HTMLImageElement|null,fitMode:"contain"|"cover",size:number):ExportCrop{const full={x:0,y:0,width:size,height:size},sourceWidth=img?.naturalWidth||img?.width||0,sourceHeight=img?.naturalHeight||img?.height||0;if(!img||fitMode==="cover"||!sourceWidth||!sourceHeight||sourceWidth===sourceHeight)return full;const evenFloor=(value:number)=>{const floored=Math.max(2,Math.floor(value));return floored%2===0?floored:floored-1};if(sourceWidth>sourceHeight){const height=evenFloor(size*sourceHeight/sourceWidth);return{x:0,y:Math.ceil((size-height)/2),width:size,height}}const width=evenFloor(size*sourceWidth/sourceHeight);return{x:Math.ceil((size-width)/2),y:0,width,height:size}}
function cropFrame(source:HTMLCanvasElement,crop:ExportCrop){if(crop.x===0&&crop.y===0&&crop.width===source.width&&crop.height===source.height)return source;const output=document.createElement("canvas");output.width=crop.width;output.height=crop.height;output.getContext("2d")!.drawImage(source,crop.x,crop.y,crop.width,crop.height,0,0,crop.width,crop.height);source.width=source.height=1;return output}
function yieldAfterPaint(){return new Promise<void>(resolve=>requestAnimationFrame(()=>setTimeout(resolve,0)))}
function requestDemoImage(kind:MotionKind,onload:(image:HTMLImageElement)=>void){const image=new Image();image.onload=()=>onload(image);image.src=DEMO_IMAGES[kind]}

export default function Home(){
 const canvasRef=useRef<HTMLCanvasElement>(null),previewBufferRef=useRef<HTMLCanvasElement|null>(null),sourceRef=useRef<HTMLCanvasElement|null>(null),originalImageRef=useRef<HTMLImageElement|null>(null),pointsRef=useRef<Point[]>(INITIAL_POINTS),pointRailRef=useRef<HTMLDivElement>(null),draftRef=useRef<Vec[]>([]),draftCurveRef=useRef(true),dragRef=useRef<{id:number;lastX:number;lastY:number;vertex?:number;lightHandle?:boolean;rippleHandle?:boolean;wholeRegion?:boolean;started?:boolean;startClientX?:number;startClientY?:number}|null>(null),autoRef=useRef(true),speedRef=useRef(50),rippleIntervalRef=useRef(3.4),phaseRef=useRef(0),rafRef=useRef(0),previewVisibleRef=useRef(true),scrollingRef=useRef(false),scrollTimerRef=useRef<ReturnType<typeof setTimeout>|null>(null),dragDepthRef=useRef(0),historyRef=useRef<Point[][]>([]),pointHintRef=useRef(true),readyRef=useRef<ReadyExport|null>(null),editRevisionRef=useRef(0),pointIdsRef=useRef("1,2"),previousRippleIntervalRef=useRef(3.4),wasClearedRef=useRef(false),modeStatesRef=useRef<Record<MotionKind,ModeEditorState>>({breath:initialModeState("breath"),flutter:initialModeState("flutter"),light:initialModeState("light"),ripple:initialModeState("ripple")});
 const [points,setPoints]=useState<Point[]>(INITIAL_POINTS),[draft,setDraft]=useState<Vec[]>([]),[draftCurve,setDraftCurve]=useState(true),[showGuides,setShowGuides]=useState(true),[selected,setSelected]=useState<number|null>(1),[auto,setAuto]=useState(true),[motionSpeed,setMotionSpeed]=useState(50),[rippleInterval,setRippleInterval]=useState(3.4),[outputSize,setOutputSize]=useState<512|720|1080>(720),[fitMode,setFitMode]=useState<"contain"|"cover">("contain"),[status,setStatus]=useState("「新しい範囲」で揺らす場所を囲んでください"),[exporting,setExporting]=useState(false),[exportProgress,setExportProgress]=useState(0),[selectedFormat,setSelectedFormat]=useState<ExportKind|null>(null),[ready,setReady]=useState<ReadyExport|null>(null),[hasCustom,setHasCustom]=useState(false),[isDragging,setIsDragging]=useState(false),[historySize,setHistorySize]=useState(0),[isCleared,setIsCleared]=useState(false),[effectMode,setEffectMode]=useState<MotionKind>("breath"),[hasAdjustedPoint,setHasAdjustedPoint]=useState(false),[mobilePanel,setMobilePanel]=useState<"image"|"motion">("image"),[showAdvanced,setShowAdvanced]=useState(false);
 const [pointRailState,setPointRailState]=useState({overflow:false,left:false,right:false});
 const updatePointRail=useCallback(()=>{const rail=pointRailRef.current;if(!rail)return;const overflow=rail.scrollWidth>rail.clientWidth+1,left=overflow&&rail.scrollLeft>1,right=overflow&&rail.scrollLeft+rail.clientWidth<rail.scrollWidth-1;setPointRailState(current=>current.overflow===overflow&&current.left===left&&current.right===right?current:{overflow,left,right})},[]);
 const scrollPointRail=(direction:-1|1)=>{const rail=pointRailRef.current;if(!rail)return;rail.scrollBy({left:direction*Math.max(80,rail.clientWidth*.72),behavior:"smooth"})};
 const discardReady=useCallback(()=>{const current=readyRef.current;if(current)URL.revokeObjectURL(current.url);readyRef.current=null;setReady(null);setExportProgress(0)},[]);
 const markEdited=useCallback(()=>{editRevisionRef.current+=1;if(readyRef.current)setStatus("設定を変更しました。現在の内容でもう一度生成してください");discardReady()},[discardReady]);
 const restoreDemo=useCallback(()=>{markEdited();const resetStates:Record<MotionKind,ModeEditorState>={breath:initialModeState("breath"),flutter:initialModeState("flutter"),light:initialModeState("light"),ripple:initialModeState("ripple")};modeStatesRef.current=resetStates;const restored=clonePoints(resetStates[effectMode].points);pointsRef.current=restored;setPoints(restored);setSelected(resetStates[effectMode].selected);setDraft([]);draftRef.current=[];setMotionSpeed(50);speedRef.current=50;setRippleInterval(3.4);rippleIntervalRef.current=3.4;requestDemoImage(effectMode,image=>{originalImageRef.current=image;sourceRef.current=fitImage(image,false);setHasCustom(false);setIsCleared(false);setFitMode("contain");setHasAdjustedPoint(false);pointHintRef.current=true;historyRef.current=[];setHistorySize(0);setStatus(DEMO_STATUSES[effectMode])})},[effectMode,markEdited]);
 useEffect(()=>{pointsRef.current=points},[points]);useEffect(()=>{draftRef.current=draft},[draft]);useEffect(()=>{draftCurveRef.current=draftCurve},[draftCurve]);useEffect(()=>{autoRef.current=auto},[auto]);useEffect(()=>{speedRef.current=motionSpeed},[motionSpeed]);useEffect(()=>{rippleIntervalRef.current=rippleInterval},[rippleInterval]);useEffect(()=>{readyRef.current=ready},[ready]);
 const pointIdSignature=points.map(point=>point.id).join(",");
 useEffect(()=>{if(pointIdsRef.current===pointIdSignature)return;pointIdsRef.current=pointIdSignature;markEdited()},[pointIdSignature,markEdited]);
 useEffect(()=>{if(previousRippleIntervalRef.current===rippleInterval)return;previousRippleIntervalRef.current=rippleInterval;markEdited()},[rippleInterval,markEdited]);
 useEffect(()=>{if(isCleared&&!wasClearedRef.current){const emptyState=(kind:MotionKind):ModeEditorState=>({...initialModeState(kind),points:[],selected:null});modeStatesRef.current={breath:emptyState("breath"),flutter:emptyState("flutter"),light:emptyState("light"),ripple:emptyState("ripple")};markEdited()}wasClearedRef.current=isCleared},[isCleared,markEdited]);
 useEffect(()=>{updatePointRail();window.addEventListener("resize",updatePointRail);return()=>window.removeEventListener("resize",updatePointRail)},[updatePointRail]);
 useEffect(()=>{const frame=requestAnimationFrame(()=>{const rail=pointRailRef.current,button=selected===null?null:rail?.querySelector<HTMLElement>(`[data-point-id="${selected}"]`);if(rail&&button){const left=button.offsetLeft,right=left+button.offsetWidth;if(left<rail.scrollLeft)rail.scrollTo({left:Math.max(0,left-6),behavior:"smooth"});else if(right>rail.scrollLeft+rail.clientWidth)rail.scrollTo({left:right-rail.clientWidth+6,behavior:"smooth"})}updatePointRail()});return()=>cancelAnimationFrame(frame)},[points.length,selected,updatePointRail]);
 useEffect(()=>{const canvas=canvasRef.current;if(!canvas)return;const observer=new IntersectionObserver(entries=>{previewVisibleRef.current=entries[0]?.isIntersecting??true},{threshold:.01}),stopScrollPause=()=>{scrollingRef.current=false;scrollTimerRef.current=null},pauseForScroll=()=>{scrollingRef.current=true;if(scrollTimerRef.current!==null)clearTimeout(scrollTimerRef.current);scrollTimerRef.current=setTimeout(stopScrollPause,180)};observer.observe(canvas);window.addEventListener("scroll",pauseForScroll,{passive:true});window.visualViewport?.addEventListener("scroll",pauseForScroll,{passive:true});return()=>{observer.disconnect();window.removeEventListener("scroll",pauseForScroll);window.visualViewport?.removeEventListener("scroll",pauseForScroll);if(scrollTimerRef.current!==null)clearTimeout(scrollTimerRef.current)}},[]);
 useEffect(()=>{if(hasCustom||isCleared)return;let cancelled=false;requestDemoImage(effectMode,image=>{if(cancelled)return;originalImageRef.current=image;sourceRef.current=fitImage(image,false);setFitMode("contain");setHasAdjustedPoint(false);pointHintRef.current=true;historyRef.current=[];setHistorySize(0);setStatus(DEMO_STATUSES[effectMode])});return()=>{cancelled=true}},[effectMode,hasCustom,isCleared]);
 const draw=useCallback((exportCtx?:CanvasRenderingContext2D,forcedPhase?:number,exportSource?:HTMLCanvasElement|null,exportSpeeds?:Map<number,number>)=>{
  const canvas=canvasRef.current,src=exportSource??sourceRef.current;if(!canvas||!src)return;
  let previewBuffer=previewBufferRef.current;if(!exportCtx&&!previewBuffer){previewBuffer=document.createElement("canvas");previewBuffer.width=W;previewBuffer.height=H;previewBufferRef.current=previewBuffer}const g=exportCtx||previewBuffer!.getContext("2d")!,phase=forcedPhase??phaseRef.current,mobileCanvas=canvas.clientWidth<=520;
  g.clearRect(0,0,W,H);g.fillStyle="#eef2eb";g.fillRect(0,0,W,H);g.drawImage(src,0,0,W,H);
  const current=pointsRef.current,moving=autoRef.current||forcedPhase!==undefined,exportingFrame=forcedPhase!==undefined,rippleClock=exportingFrame?phase/(Math.PI*2)*rippleIntervalRef.current:phase/(Math.PI*2)*1.5,rippleStates=new Map(current.map(point=>[point.id,moving?rippleWaveStates(rippleClock,point,rippleIntervalRef.current,speedRef.current):[]]));
  const displaced=(x:number,y:number)=>{let ox=0,oy=0;for(const p of current){
   const localPhase=motionPhase(phase,p,speedRef.current,exportSpeeds?.get(p.id)),edge=Math.min(1,Math.max(0,Math.min(x,y,W-x,H-y)/32));
   if(effectMode==="ripple"){
    const cx=p.x+p.dx,cy=p.y+p.dy,rx=x-cx,ry=y-cy,distance=Math.hypot(rx,ry),reach=Math.max(28,p.radius),waves=rippleStates.get(p.id)??[];if(!moving||!waves.length||distance>=reach||distance<.001)continue;
    const spacing=Math.max(12,p.stiffness),packetWidth=spacing*(.45+Math.max(0,Math.min(80,p.feather))/80*.65),centerFade=Math.min(1,distance/Math.max(6,spacing*.35)),impact=Math.max(0,Math.min(1,(p.impact??25)/100));let wave=0;
    for(const state of waves){const front=state.progress*(reach+spacing*1.15),offset=distance-front,envelope=Math.exp(-Math.pow(offset/Math.max(6,packetWidth),2)*1.35),carrier=Math.sin(offset/spacing*Math.PI*2);wave+=carrier*envelope*p.amplitude*state.life*centerFade;if(impact>0&&state.progress<.13){const impactLife=Math.sin(state.progress/.13*Math.PI),impactRadius=Math.max(5,spacing*(.16+state.progress*2)),impactEnvelope=Math.exp(-Math.pow(distance/impactRadius,2)*1.8);wave-=impactEnvelope*impactLife*impact*4.5}}
    wave=Math.max(-p.amplitude*1.8,Math.min(p.amplitude*1.8,wave));
    ox+=rx/distance*wave*edge;oy+=ry/distance*wave*edge;continue;
   }
    const radial=Math.exp(-((x-p.x)**2+(y-p.y)**2)/(2*p.radius*p.radius)),shape=regionShape(p),weight=shape?polygonWeight(x,y,shape,p.feather,p.outer):radial,elasticity=(90-p.stiffness)/85,bounce=.14+(p.damping-30)/68*.38,breathWave=moving?(Math.sin(localPhase)+Math.sin(localPhase*2-.45)*elasticity*bounce)*p.amplitude:0,flutterWave=moving?(Math.sin(localPhase-x/W*Math.PI*2.2)+Math.sin(localPhase*2-x/W*Math.PI*1.15+y/H*.65)*elasticity*bounce)*p.amplitude:0,whole=Math.sin(localPhase)*p.whole;
   if(effectMode==="flutter"){ox+=(p.dx+flutterWave+whole*.45)*weight*edge;oy+=(p.dy+flutterWave*.18)*weight*edge}else{ox+=(p.dx+breathWave*.28+whole)*weight*edge;oy+=(p.dy+breathWave)*weight*edge}
  }return{x:x+ox,y:y+oy}};
  const tri=(a:[number,number],b:[number,number],c:[number,number])=>{const A=displaced(...a),B=displaced(...b),C=displaced(...c),den=a[0]*(b[1]-c[1])+b[0]*(c[1]-a[1])+c[0]*(a[1]-b[1]);if(!den)return;const m11=(A.x*(b[1]-c[1])+B.x*(c[1]-a[1])+C.x*(a[1]-b[1]))/den,m12=(A.y*(b[1]-c[1])+B.y*(c[1]-a[1])+C.y*(a[1]-b[1]))/den,m21=(A.x*(c[0]-b[0])+B.x*(a[0]-c[0])+C.x*(b[0]-a[0]))/den,m22=(A.y*(c[0]-b[0])+B.y*(a[0]-c[0])+C.y*(b[0]-a[0]))/den,dx=(A.x*(b[0]*c[1]-c[0]*b[1])+B.x*(c[0]*a[1]-a[0]*c[1])+C.x*(a[0]*b[1]-b[0]*a[1]))/den,dy=(A.y*(b[0]*c[1]-c[0]*b[1])+B.y*(c[0]*a[1]-a[0]*c[1])+C.y*(a[0]*b[1]-b[0]*a[1]))/den;g.save();const cx=(A.x+B.x+C.x)/3,cy=(A.y+B.y+C.y)/3,expand=(p:{x:number,y:number})=>({x:cx+(p.x-cx)*1.12,y:cy+(p.y-cy)*1.12}),EA=expand(A),EB=expand(B),EC=expand(C);g.beginPath();g.moveTo(EA.x,EA.y);g.lineTo(EB.x,EB.y);g.lineTo(EC.x,EC.y);g.closePath();g.clip();g.transform(m11,m12,m21,m22,dx,dy);g.drawImage(src,0,0,W,H);g.restore()};
  if(effectMode!=="light"){const meshStep=effectMode==="ripple"?18:GRID;for(let y=0;y<H;y+=meshStep)for(let x=0;x<W;x+=meshStep){const x2=Math.min(x+meshStep,W),y2=Math.min(y+meshStep,H);tri([x,y],[x2,y],[x,y2]);tri([x2,y],[x2,y2],[x,y2])}}
  if(effectMode==="light"){
   g.save();g.globalCompositeOperation="screen";
   for(const p of current){const px=p.x+p.dx,py=p.y+p.dy,{aspect,angle}=lightGeometry(p),localPhase=motionPhase(phase,p,speedRef.current,exportSpeeds?.get(p.id)),irregular=(Math.sin(localPhase*2)+Math.sin(localPhase*4+1.2)*.45+Math.sin(localPhase*7+.4)*.18)/1.63,flicker=Math.max(0,Math.min(1,(p.damping-30)/68)),motionAmount=.18+flicker*.28,pulse=moving?1+irregular*motionAmount:1,intensity=Math.max(0,p.amplitude/60)*pulse,warmth=Math.max(0,Math.min(1,(p.stiffness-5)/85)),baseRadius=Math.max(20,p.radius),radius=baseRadius*(1+(moving?irregular*(.05+flicker*.07):0)),glowX=px+(moving?Math.sin(localPhase*5+.8)*(1+flicker*2.2):0),glowY=py+(moving?irregular*(-3-flicker*8):0),red=255,green=Math.round(236-warmth*56),blue=Math.round(190-warmth*96),alpha=Math.min(.78,.1+intensity*.62),softness=Math.max(.2,Math.min(.58,.2+p.feather/158)),spill=Math.max(0,Math.min(1,p.whole/60));g.save();g.translate(glowX,glowY);g.rotate(angle);g.scale(aspect,1);const gradient=g.createRadialGradient(0,0,0,0,0,radius);gradient.addColorStop(0,`rgba(${red},${green},${blue},${alpha})`);gradient.addColorStop(softness,`rgba(${red},${green},${blue},${alpha*(.36+spill*.22)})`);gradient.addColorStop(.72,`rgba(${red},${green},${blue},${alpha*(.08+spill*.12)})`);gradient.addColorStop(1,`rgba(${red},${green},${blue},0)`);g.fillStyle=gradient;g.fillRect(-radius,-radius,radius*2,radius*2);g.restore();g.globalCompositeOperation="source-over";const tintAlpha=Math.min(.38,.05+intensity*.42),tintRadius=radius*.82;g.save();g.translate(glowX,glowY);g.rotate(angle);g.scale(aspect,1);const tint=g.createRadialGradient(0,0,0,0,0,tintRadius);tint.addColorStop(0,`rgba(${red},${green},${blue},${tintAlpha})`);tint.addColorStop(.42,`rgba(${red},${green},${blue},${tintAlpha*.38})`);tint.addColorStop(1,`rgba(${red},${green},${blue},0)`);g.fillStyle=tint;g.fillRect(-tintRadius,-tintRadius,tintRadius*2,tintRadius*2);g.restore();g.globalCompositeOperation="screen"}
   g.restore();
  }
  if(effectMode==="ripple"&&moving){
   for(const p of current){const{cx,cy,radius}=rippleGeometry(p),spacing=Math.max(12,p.stiffness),strength=Math.max(0,Math.min(1,p.whole/60)),impact=Math.max(0,Math.min(1,(p.impact??25)/100));for(const state of rippleStates.get(p.id)??[]){const front=state.progress*(radius+spacing*1.15),shadowRadius=front-spacing*.22,highlightRadius=front+spacing*.22;
    if(impact>0&&state.progress<.13){const impactLife=Math.sin(state.progress/.13*Math.PI)*impact,dropRadius=2.5+state.progress*spacing*.8;g.save();const dimple=g.createRadialGradient(cx,cy,0,cx,cy,dropRadius*2.4);dimple.addColorStop(0,`rgba(24,72,78,${.12*impactLife})`);dimple.addColorStop(.55,`rgba(24,72,78,${.045*impactLife})`);dimple.addColorStop(1,"rgba(24,72,78,0)");g.fillStyle=dimple;g.beginPath();g.arc(cx,cy,dropRadius*2.4,0,Math.PI*2);g.fill();g.globalCompositeOperation="screen";g.beginPath();g.arc(cx,cy,dropRadius,0,Math.PI*2);g.strokeStyle=`rgba(248,253,247,${.16*impactLife})`;g.lineWidth=1+impact*1.2;g.stroke();g.restore()}
    if(shadowRadius>2&&shadowRadius<radius){g.save();g.globalCompositeOperation="source-over";g.beginPath();g.arc(cx,cy,shadowRadius,0,Math.PI*2);g.strokeStyle=`rgba(27,82,87,${(.018+strength*.035)*state.life})`;g.lineWidth=1.4+strength*1.8;g.stroke();g.restore()}
    if(highlightRadius>2&&highlightRadius<radius){g.save();g.globalCompositeOperation="screen";g.beginPath();g.arc(cx,cy,highlightRadius,0,Math.PI*2);g.strokeStyle=`rgba(244,251,242,${(.028+strength*.06)*state.life})`;g.lineWidth=1.1+strength*1.7;g.stroke();g.restore()}
   }
   }
  }
  if(!exportCtx&&showGuides){
   if(effectMode==="light")current.forEach(p=>{if(p.id!==selected)return;const{aspect,angle,cx,cy,handleX,handleY}=lightGeometry(p);g.save();g.beginPath();g.ellipse(cx,cy,p.radius*aspect,p.radius,angle,0,Math.PI*2);g.setLineDash([8,8]);g.strokeStyle="rgba(173,148,101,.78)";g.lineWidth=2;g.stroke();g.setLineDash([]);g.beginPath();g.moveTo(cx,cy);g.lineTo(handleX,handleY);g.strokeStyle="rgba(173,148,101,.72)";g.lineWidth=1.5;g.stroke();g.beginPath();g.arc(handleX,handleY,mobileCanvas?16:7,0,Math.PI*2);g.fillStyle="#fff8e8";g.fill();g.strokeStyle="#ad9465";g.lineWidth=2;g.stroke();g.restore()});
   if(effectMode==="ripple")current.forEach(p=>{if(p.id!==selected)return;const{cx,cy,radius,handleX,handleY}=rippleGeometry(p);g.save();g.beginPath();g.arc(cx,cy,radius,0,Math.PI*2);g.setLineDash([8,8]);g.strokeStyle="rgba(47,108,113,.72)";g.lineWidth=2;g.stroke();g.setLineDash([]);g.beginPath();g.moveTo(cx,cy);g.lineTo(handleX,handleY);g.strokeStyle="rgba(47,108,113,.65)";g.lineWidth=1.5;g.stroke();g.beginPath();g.arc(handleX,handleY,mobileCanvas?16:7,0,Math.PI*2);g.fillStyle="#f4faf7";g.fill();g.strokeStyle="#2f6c71";g.lineWidth=2;g.stroke();g.restore()});
   current.forEach(p=>{if(p.id===selected||!p.polygon)return;const outline=regionShape(p)!;g.save();g.beginPath();outline.forEach((v,i)=>i?g.lineTo(v.x,v.y):g.moveTo(v.x,v.y));g.closePath();g.setLineDash([6,6]);g.strokeStyle="rgba(168,92,88,.62)";g.lineWidth=1.5;g.stroke();g.restore()});
   const selectedPoint=current.find(p=>p.id===selected);if(selectedPoint?.polygon){g.save();const outline=regionShape(selectedPoint)!;g.beginPath();outline.forEach((v,i)=>i?g.lineTo(v.x,v.y):g.moveTo(v.x,v.y));g.closePath();g.fillStyle="#b8432f26";g.fill();g.strokeStyle="#b8432f";g.lineWidth=3;g.stroke();selectedPoint.polygon.forEach((v,index)=>{if(pointHintRef.current&&phase<Math.PI*2){const pulse=(Math.sin(phase*2.2-index*.72)+1)/2;g.beginPath();g.arc(v.x,v.y,(mobileCanvas?18:10)+pulse*(mobileCanvas?8:5),0,Math.PI*2);g.strokeStyle=`rgba(184,67,47,${.16+pulse*.32})`;g.lineWidth=2;g.stroke()}g.beginPath();g.arc(v.x,v.y,mobileCanvas?13:6,0,Math.PI*2);g.fillStyle="#fff";g.fill();g.strokeStyle="#b8432f";g.lineWidth=2;g.stroke()});g.restore()}
   const creating=draftRef.current;if(creating.length){g.save();g.beginPath();creating.forEach((v,i)=>i?g.lineTo(v.x,v.y):g.moveTo(v.x,v.y));g.strokeStyle="#b8432f";g.lineWidth=3;g.setLineDash([8,6]);g.stroke();g.setLineDash([]);for(const v of creating){g.beginPath();g.arc(v.x,v.y,mobileCanvas?13:7,0,Math.PI*2);g.fillStyle="#b8432f";g.fill();g.strokeStyle="#fff";g.lineWidth=2;g.stroke()}g.restore()}
   current.forEach((p,index)=>{const px=p.x+p.dx,py=p.y+p.dy;g.beginPath();g.arc(px,py,mobileCanvas?(selected===p.id?24:21):(selected===p.id?13:11),0,Math.PI*2);g.fillStyle=effectMode==="light"?(selected===p.id?"#bd7541":"rgba(173,148,101,.88)"):effectMode==="ripple"?(selected===p.id?"#2f6c71":"rgba(47,108,113,.82)"):(selected===p.id?"#b8432f":"rgba(40,52,47,.82)");g.fill();g.strokeStyle="#fff";g.lineWidth=2;g.stroke();g.fillStyle="#fff";g.font=`700 ${mobileCanvas?15:10}px system-ui`;g.textAlign="center";g.textBaseline="middle";g.fillText(String(index+1),px,py+.5)})
  }
  if(!exportCtx&&previewBuffer){const visible=canvas.getContext("2d")!;visible.save();visible.globalCompositeOperation="copy";visible.drawImage(previewBuffer,0,0);visible.restore()}
 },[effectMode,selected,showGuides]);
 useEffect(()=>{if(!sourceRef.current)sourceRef.current=makeDemo();let last=performance.now();const tick=(now:number)=>{const elapsed=Math.min((now-last)/1000,.25);last=now;if(!previewVisibleRef.current||scrollingRef.current){rafRef.current=requestAnimationFrame(tick);return}const dt=Math.min(elapsed,.035);phaseRef.current+=elapsed*Math.PI*2/1.5;let changed=false;const next=pointsRef.current.map(p=>{if(dragRef.current?.id===p.id)return p;let{dx,dy,vx,vy}=p;vx+=-dx*p.stiffness*dt;vy+=-dy*p.stiffness*dt;const damp=Math.pow(p.damping/100,dt*7);vx*=damp;vy*=damp;dx+=vx*dt;dy+=vy*dt;if(Math.abs(dx)+Math.abs(dy)>.01)changed=true;return{...p,dx,dy,vx,vy}});if(changed){pointsRef.current=next;setPoints(next)}draw();rafRef.current=requestAnimationFrame(tick)};rafRef.current=requestAnimationFrame(tick);return()=>cancelAnimationFrame(rafRef.current)},[draw]);
 const pos=(e:ReactPointerEvent<HTMLCanvasElement>)=>{const r=e.currentTarget.getBoundingClientRect();return{x:(e.clientX-r.left)*W/r.width,y:(e.clientY-r.top)*H/r.height}};
 const saveHistory=()=>{historyRef.current=[...historyRef.current.slice(-29),clonePoints(pointsRef.current)];setHistorySize(historyRef.current.length)};
 const undo=()=>{const previous=historyRef.current.pop();if(!previous)return;markEdited();const restored=previous.map(p=>({...p,vx:0,vy:0}));pointsRef.current=restored;setPoints(restored);setSelected(restored[0]?.id??null);setHistorySize(historyRef.current.length);setStatus("ひとつ前の範囲設定に戻しました")};
 const startRegion=()=>{setMobilePanel("motion");setDraft([]);draftRef.current=[];setDraftCurve(true);draftCurveRef.current=true;setShowGuides(true);setSelected(null);setStatus(effectMode==="light"?"画像の灯したい場所をタップしてください":effectMode==="ripple"?"波紋を広げたい中心をタップしてください":"画像を順番にクリックして囲んでください。3点以上で確定できます")};
 const undoDraft=()=>{const next=draftRef.current.slice(0,-1);draftRef.current=next;setDraft(next);setStatus(next.length?`最後の点を取り消しました（現在${next.length}点）`:"置いた点をすべて取り消しました")};
 const cancelRegion=()=>{setDraft([]);draftRef.current=[];setStatus("範囲作成をキャンセルしました")};
 const finishRegion=()=>{const polygon=draftRef.current;if(polygon.length<3){setStatus("範囲の確定には3点以上必要です");return}saveHistory();markEdited();const center=polygon.reduce((a,v)=>({x:a.x+v.x/polygon.length,y:a.y+v.y/polygon.length}),{x:0,y:0}),point:Point={id:Date.now(),x:center.x,y:center.y,dx:0,dy:0,vx:0,vy:0,amplitude:4,stiffness:48,damping:effectMode==="flutter"?68:62,radius:90,feather:14,outer:2,whole:0,speed:motionSpeed,synced:false,phaseOffset:(Date.now()%997)/997*Math.PI*2,curve:draftCurveRef.current,polygon:polygon.map(v=>({...v}))},next=[...pointsRef.current,point];pointsRef.current=next;setPoints(next);setSelected(point.id);setDraft([]);draftRef.current=[];setStatus("揺れる範囲を追加しました。数字で全体を移動し、白い頂点で形を調整できます")};
 const pointerDown=(e:ReactPointerEvent<HTMLCanvasElement>)=>{
  const q=pos(e),mobileCanvas=e.currentTarget.clientWidth<=520,vertexHit=mobileCanvas?46:22,pointHit=mobileCanvas?50:28;
  if((effectMode==="light"||effectMode==="ripple")&&selected===null){saveHistory();markEdited();const point:Point=effectMode==="light"?{id:Date.now(),x:q.x,y:q.y,dx:0,dy:0,vx:0,vy:0,amplitude:34,stiffness:72,damping:64,radius:142,feather:30,outer:0,whole:18,speed:motionSpeed,synced:false,lightAspect:1,lightAngle:0,phaseOffset:(Date.now()%997)/997*Math.PI*2}:{id:Date.now(),x:q.x,y:q.y,dx:0,dy:0,vx:0,vy:0,amplitude:2.6,stiffness:42,damping:72,radius:118,feather:26,outer:0,whole:8,speed:motionSpeed,synced:false,phaseOffset:0,impact:25};let next=[...pointsRef.current,point];if(effectMode==="ripple"&&next.length>1)next=next.map((value,index)=>({...value,synced:index===0,phaseOffset:index/next.length*Math.PI*2,speed:motionSpeed}));pointsRef.current=next;setPoints(next);setSelected(point.id);setStatus(effectMode==="light"?"灯りを置きました。点を動かし、強さと広がりを整えてください":"波紋を置きました。複数の中心は順に広がります");return}
  if(selected===null){const next=[...draftRef.current,q];draftRef.current=next;setDraft(next);setStatus(`${next.length}点を追加しました${next.length>=3?"。「この範囲で決定」を押せます":""}`);return}
  const active=pointsRef.current.find(p=>p.id===selected),vertex=showGuides?(active?.polygon?.findIndex(v=>Math.hypot(q.x-v.x,q.y-v.y)<vertexHit)??-1):-1,lightHandle=showGuides&&effectMode==="light"&&active?lightGeometry(active):null,rippleHandle=showGuides&&effectMode==="ripple"&&active?rippleGeometry(active):null,handleHit=mobileCanvas?52:16;
  if(active&&lightHandle&&Math.hypot(q.x-lightHandle.handleX,q.y-lightHandle.handleY)<handleHit){saveHistory();markEdited();pointHintRef.current=false;setHasAdjustedPoint(true);dragRef.current={id:active.id,lastX:q.x,lastY:q.y,lightHandle:true};setStatus("外側のつまみで、灯りの楕円と向きを整えています");e.currentTarget.style.cursor="grabbing";e.currentTarget.setPointerCapture(e.pointerId);return}
  if(active&&rippleHandle&&Math.hypot(q.x-rippleHandle.handleX,q.y-rippleHandle.handleY)<handleHit){saveHistory();markEdited();pointHintRef.current=false;setHasAdjustedPoint(true);dragRef.current={id:active.id,lastX:q.x,lastY:q.y,rippleHandle:true};setStatus("外側のつまみで、波紋の届く範囲を整えています");e.currentTarget.style.cursor="grabbing";e.currentTarget.setPointerCapture(e.pointerId);return}
  if(active&&vertex>=0){saveHistory();markEdited();pointHintRef.current=false;setHasAdjustedPoint(true);dragRef.current={id:active.id,lastX:q.x,lastY:q.y,vertex};e.currentTarget.style.cursor="grabbing";e.currentTarget.setPointerCapture(e.pointerId);return}
  const point=showGuides?[...pointsRef.current].reverse().find(p=>Math.hypot(q.x-(p.x+p.dx),q.y-(p.y+p.dy))<pointHit):undefined;if(point){setSelected(point.id);dragRef.current={id:point.id,lastX:q.x,lastY:q.y,wholeRegion:true,started:false,startClientX:e.clientX,startClientY:e.clientY};e.currentTarget.setPointerCapture(e.pointerId);return}
  const region=[...pointsRef.current].reverse().find(p=>{const shape=regionShape(p);return Boolean(shape&&insidePolygon(q.x,q.y,shape))});if(region){setSelected(region.id);setStatus("範囲を選択しました。数字をドラッグすると全体を移動できます");return}
  setStatus(effectMode==="ripple"?"数字で中心を、外側のつまみで届く範囲を動かせます":"新しい範囲ボタンを押して、揺らす場所を囲んでください")
 };
 const pointerMove=(e:ReactPointerEvent<HTMLCanvasElement>)=>{
  if(!dragRef.current){const q=pos(e),mobileCanvas=e.currentTarget.clientWidth<=520,vertexHit=mobileCanvas?46:22,pointHit=mobileCanvas?50:28,active=pointsRef.current.find(p=>p.id===selected),lightHandle=showGuides&&effectMode==="light"&&active?lightGeometry(active):null,rippleHandle=showGuides&&effectMode==="ripple"&&active?rippleGeometry(active):null,handleHit=mobileCanvas?52:16,overLightHandle=Boolean(lightHandle&&Math.hypot(q.x-lightHandle.handleX,q.y-lightHandle.handleY)<handleHit),overRippleHandle=Boolean(rippleHandle&&Math.hypot(q.x-rippleHandle.handleX,q.y-rippleHandle.handleY)<handleHit),overVertex=showGuides&&active?.polygon?.some(v=>Math.hypot(q.x-v.x,q.y-v.y)<vertexHit),overPoint=showGuides&&pointsRef.current.some(p=>Math.hypot(q.x-(p.x+p.dx),q.y-(p.y+p.dy))<pointHit);e.currentTarget.style.cursor=overLightHandle||overRippleHandle||overVertex||overPoint?"grab":"crosshair";return}
  const q=pos(e),d=dragRef.current;
  if(d.wholeRegion&&!d.started){if(Math.hypot(e.clientX-(d.startClientX??e.clientX),e.clientY-(d.startClientY??e.clientY))<6)return;saveHistory();markEdited();d.started=true;pointHintRef.current=false;setHasAdjustedPoint(true);setStatus(effectMode==="light"?"灯り全体を移動しています":effectMode==="ripple"?"波紋の中心を移動しています":"範囲全体を移動しています");e.currentTarget.style.cursor="grabbing"}
  const n=pointsRef.current.map(p=>{if(p.id!==d.id)return p;if(d.lightHandle){const cx=p.x+p.dx,cy=p.y+p.dy,distance=Math.hypot(q.x-cx,q.y-cy),aspect=Math.max(.35,Math.min(2.5,distance/Math.max(20,p.radius)));return{...p,lightAspect:aspect,lightAngle:Math.atan2(q.y-cy,q.x-cx)}}if(d.rippleHandle){const cx=p.x+p.dx,cy=p.y+p.dy,radius=Math.max(28,Math.min(320,Math.hypot(q.x-cx,q.y-cy)));return{...p,radius}}if(d.vertex!==undefined&&p.polygon){const polygon=p.polygon.map((v,i)=>i===d.vertex?{x:q.x,y:q.y}:v);return{...p,polygon}}if(d.wholeRegion){const nextX=Math.max(28,Math.min(W-28,p.x+q.x-d.lastX)),nextY=Math.max(28,Math.min(H-28,p.y+q.y-d.lastY)),shiftX=nextX-p.x,shiftY=nextY-p.y;return{...p,x:nextX,y:nextY,dx:0,dy:0,vx:0,vy:0,polygon:p.polygon?.map(v=>({x:v.x+shiftX,y:v.y+shiftY}))}}return p});d.lastX=q.x;d.lastY=q.y;pointsRef.current=n;setPoints(n)
 };
 const chosen=points.find(p=>p.id===selected),pointValue=(key:keyof Point)=>{const value=chosen?.[key];return typeof value==="number"?value:key==="impact"?25:0},update=(key:keyof Point,value:number)=>{markEdited();const next=pointsRef.current.map(p=>p.id===selected?{...p,[key]:value}:p);pointsRef.current=next;setPoints(next)};
 const updateChosen=(changes:Partial<Point>)=>{markEdited();const next=pointsRef.current.map(point=>point.id===selected?{...point,...changes}:point);pointsRef.current=next;setPoints(next)};
 const chosenSpeed=effectMode==="ripple"?motionSpeed:chosen?.synced===false?(chosen.speed??motionSpeed):motionSpeed,changeChosenSpeed=(value:number)=>{if(!chosen)return;if(effectMode==="ripple"){markEdited();speedRef.current=value;setMotionSpeed(value);return}if(chosen.synced===false)updateChosen({speed:value});else{markEdited();speedRef.current=value;setMotionSpeed(value)}},changeRippleInterval=(value:number)=>{markEdited();rippleIntervalRef.current=value;setRippleInterval(value)};
 const loadImageFile=useCallback((file:File,source:"選択"|"ドロップ"|"貼り付け"="選択")=>{if(!file.type.startsWith("image/")){setStatus("画像ファイルを選んでください");return}const img=new Image();img.onload=()=>{markEdited();originalImageRef.current=img;sourceRef.current=fitImage(img,fitMode==="cover");setHasCustom(true);setIsCleared(false);const emptyState=(kind:MotionKind):ModeEditorState=>({...initialModeState(kind),points:[],selected:null});modeStatesRef.current={breath:emptyState("breath"),flutter:emptyState("flutter"),light:emptyState("light"),ripple:emptyState("ripple")};setPoints([]);pointsRef.current=[];historyRef.current=[];setHistorySize(0);setSelected(null);setDraft([]);draftRef.current=[];setMotionSpeed(50);speedRef.current=50;setRippleInterval(3.4);rippleIntervalRef.current=3.4;setMobilePanel("motion");setStatus(effectMode==="light"?`${source}で画像を読み込みました。「新しい灯り」を押して灯したい場所をタップしてください`:effectMode==="ripple"?`${source}で画像を読み込みました。「新しい波紋」を押して中心をタップしてください`:`${source}で画像を読み込みました。「新しい範囲」を押して動きを宿したい場所を囲んでください`);URL.revokeObjectURL(img.src)};img.onerror=()=>{setStatus("画像を読み込めませんでした");URL.revokeObjectURL(img.src)};img.src=URL.createObjectURL(file)},[fitMode,effectMode,markEdited]);
 const upload=(e:ChangeEvent<HTMLInputElement>)=>{const file=e.target.files?.[0];if(file)loadImageFile(file)};
 const changeFit=(mode:"contain"|"cover")=>{if(mode===fitMode)return;markEdited();setFitMode(mode);if(originalImageRef.current)sourceRef.current=fitImage(originalImageRef.current,mode==="cover");setStatus(mode==="cover"?"余白なしの中央クロップに変更しました":"画像全体を表示しました")};
 useEffect(()=>{const paste=(event:ClipboardEvent)=>{const item=[...(event.clipboardData?.items??[])].find(value=>value.type.startsWith("image/"));const file=item?.getAsFile();if(file){event.preventDefault();loadImageFile(file,"貼り付け")}};window.addEventListener("paste",paste);return()=>window.removeEventListener("paste",paste)},[loadImageFile]);
 const dragEnter=(event:React.DragEvent<HTMLElement>)=>{if(!event.dataTransfer.types.includes("Files"))return;event.preventDefault();dragDepthRef.current+=1;setIsDragging(true)};
 const dragLeave=(event:React.DragEvent<HTMLElement>)=>{event.preventDefault();dragDepthRef.current=Math.max(0,dragDepthRef.current-1);if(dragDepthRef.current===0)setIsDragging(false)};
 const drop=(event:React.DragEvent<HTMLElement>)=>{event.preventDefault();dragDepthRef.current=0;setIsDragging(false);const file=[...event.dataTransfer.files].find(value=>value.type.startsWith("image/"));if(file)loadImageFile(file,"ドロップ");else setStatus("画像ファイルをドロップしてください")};
 const renderFrame=(phase:number,exportSource?:HTMLCanvasElement|null,exportSpeeds?:Map<number,number>,crop?:ExportCrop)=>{const c=document.createElement("canvas");c.width=outputSize;c.height=outputSize;const context=c.getContext("2d")!;context.scale(outputSize/W,outputSize/H);draw(context,phase,exportSource,exportSpeeds);return crop?cropFrame(c,crop):c};
 const encodeMp4=async(exportSource:HTMLCanvasElement|null,cycleSeconds:number,phaseMultiplier:number,cycleFrameCount:number,progress:(frame:number,total:number)=>void,crop:ExportCrop,exportSpeeds?:Map<number,number>)=>{
  const safeCycle=Math.max(.35,cycleSeconds),framesPerCycle=Math.max(8,Math.min(96,cycleFrameCount)),loops=Math.max(2,Math.ceil(5/safeCycle)),totalFrames=framesPerCycle*loops,frameDuration=safeCycle/framesPerCycle;
  if(!await canEncodeVideo("avc",{width:crop.width,height:crop.height,quality:QUALITY_HIGH}))throw new Error("MP4_UNSUPPORTED");
  const renderCanvas=document.createElement("canvas");renderCanvas.width=outputSize;renderCanvas.height=outputSize;const renderContext=renderCanvas.getContext("2d")!;renderContext.scale(outputSize/W,outputSize/H);const canvas=document.createElement("canvas");canvas.width=crop.width;canvas.height=crop.height;const context=canvas.getContext("2d")!;
  const target=new BufferTarget(),output=new Output({format:new Mp4OutputFormat({fastStart:"in-memory"}),target}),source=new CanvasSource(canvas,{codec:"avc",quality:QUALITY_HIGH,keyFrameInterval:2,latencyMode:"quality"});output.addVideoTrack(source);
  try{await output.start();for(let i=0;i<totalFrames;i++){const cycleFrame=i%framesPerCycle,phase=cycleFrame/framesPerCycle*Math.PI*2*phaseMultiplier;draw(renderContext,phase,exportSource,exportSpeeds);context.clearRect(0,0,crop.width,crop.height);context.drawImage(renderCanvas,crop.x,crop.y,crop.width,crop.height,0,0,crop.width,crop.height);await source.add(i*frameDuration,frameDuration,{keyFrame:i===0});progress(i+1,totalFrames);if(i%4===3)await new Promise<void>(resolve=>requestAnimationFrame(()=>resolve()))}await output.finalize()}catch(err){await output.cancel();throw err}
  if(!target.buffer)throw new Error("MP4_EMPTY");return new Blob([target.buffer],{type:"video/mp4"});
 };
 const clearReady=()=>discardReady();
 const chooseOutputSize=(size:512|720|1080)=>{setOutputSize(size);clearReady();setStatus(`${size}を選択しました。生成するを押してください`)};
 const chooseExportFormat=(kind:ExportKind)=>{setSelectedFormat(kind);clearReady();setStatus(kind==="mp4"?`MP4を選択しました。${outputSize===1080?"高負荷のため生成に時間がかかります。":"生成するを押してください"}`:`${kind.toUpperCase()}を選択しました。生成するを押してください`)};
 const exportFile=async(kind:ExportKind)=>{const tying=effectMode==="light"?"一枚ずつ、灯りを結んでいます":effectMode==="ripple"?"一枚ずつ、波紋を結んでいます":"一枚ずつ、息を結んでいます",revisionAtStart=editRevisionRef.current,modeAtStart=effectMode;discardReady();setExporting(true);setExportProgress(0);setStatus(tying);await yieldAfterPaint();try{const exportSource=originalImageRef.current?fitImage(originalImageRef.current,fitMode==="cover",outputSize,outputSize):sourceRef.current,crop=exportCrop(originalImageRef.current,fitMode,outputSize),exportWidth=crop.width,exportHeight=crop.height,masterSpeed=speedRef.current,rippleExport=effectMode==="ripple",loopPlan=rippleExport?null:exportLoopPlan(pointsRef.current,masterSpeed),frames=loopPlan?.frameCount??24,cycleDuration=loopPlan?.durationMs??rippleIntervalRef.current*1000,phaseScale=loopPlan?.phaseMultiplier??1,exportSpeeds=loopPlan?.speedOverrides,delay=Math.max(16,Math.round(cycleDuration/frames)),exportPhase=(i:number)=>i/frames*Math.PI*2*phaseScale,progress=(frame:number,total=frames)=>{const percent=Math.round(frame/total*100);setExportProgress(percent);setStatus(`${tying}　${percent}%`)};let blob:Blob,name:string;if(kind==="gif"){const gif=GIFEncoder();for(let i=0;i<frames;i++){const c=renderFrame(exportPhase(i),exportSource,exportSpeeds,crop),rgba=c.getContext("2d")!.getImageData(0,0,exportWidth,exportHeight).data,palette=quantize(rgba,128);gif.writeFrame(applyPalette(rgba,palette),exportWidth,exportHeight,{palette,delay});c.width=c.height=1;progress(i+1);if(outputSize===1080||i%4===3)await yieldAfterPaint()}gif.finish();blob=new Blob([gif.bytesView().slice().buffer],{type:"image/gif"});name=datedName("gif")}else if(kind==="apng"){const buffers:ArrayBuffer[]=[];for(let i=0;i<frames;i++){const c=renderFrame(exportPhase(i),exportSource,exportSpeeds,crop),buffer=c.getContext("2d")!.getImageData(0,0,exportWidth,exportHeight).data.buffer as ArrayBuffer;buffers.push(buffer);c.width=c.height=1;progress(i+1);if(i%4===3)await new Promise<void>(resolve=>requestAnimationFrame(()=>resolve()))}blob=new Blob([UPNG.encode(buffers,exportWidth,exportHeight,0,Array(frames).fill(delay))],{type:"image/apng"});name=datedName("apng")}else if(kind==="webp"){blob=await animatedWebP(frames,delay,exportWidth,exportHeight,phase=>renderFrame(phase*phaseScale,exportSource,exportSpeeds,crop),progress);name=datedName("webp")}else{blob=await encodeMp4(exportSource,cycleDuration/1000,phaseScale,frames,progress,crop,exportSpeeds);name=datedName("mp4")}if(editRevisionRef.current!==revisionAtStart||effectMode!==modeAtStart){setExportProgress(0);setStatus("生成中に設定が変わりました。もう一度生成してください");return}const completed:ReadyExport={url:URL.createObjectURL(blob),blob,name,label:kind.toUpperCase(),revision:revisionAtStart,mode:modeAtStart};readyRef.current=completed;setReady(completed);setExportProgress(100);setStatus(`${kind.toUpperCase()}の作品ができあがりました`)}catch(err){console.error(err);setStatus(kind==="mp4"?"この端末ではMP4を生成できません。GIFをお試しください":"書き出しに失敗しました。別の形式でもお試しください")}finally{setExporting(false)}};
 const validReady=()=>{const current=readyRef.current;if(!current||current.revision!==editRevisionRef.current||current.mode!==effectMode){discardReady();setStatus("設定が変わりました。現在の内容でもう一度生成してください");return null}return current};
 const saveReadyFile=async()=>{const current=validReady();if(!current)return;const picker=(window as PickerWindow).showSaveFilePicker;if(!picker){const a=document.createElement("a");a.href=current.url;a.download=current.name;document.body.appendChild(a);a.click();a.remove();setStatus("このブラウザでは保存先を選べないため、ダウンロードしました");return}try{const extension=`.${current.name.split(".").pop()}`;const handle=await picker.call(window,{suggestedName:current.name,types:[{description:`${current.label} animation`,accept:{[current.blob.type]:[extension]}}]});const writable=await handle.createWritable();await writable.write(current.blob);await writable.close();setStatus(`${current.label}を選択した場所へ保存しました`)}catch(err){if(err instanceof DOMException&&err.name==="AbortError")setStatus("保存をキャンセルしました");else{console.error(err);setStatus("保存に失敗しました")}}};
 const shareReadyFile=async()=>{const current=validReady();if(!current)return;const shareNavigator=navigator as ShareNavigator,file=new File([current.blob],current.name,{type:current.blob.type,lastModified:Date.now()}),files=[file];if(!shareNavigator.share||!shareNavigator.canShare||!shareNavigator.canShare({files})){setStatus("この端末では直接共有できません。保存してから共有してください");return}try{await shareNavigator.share({files,title:"YURAMEKI",text:"静かな気配を、一枚の絵に。\nYURAMEKIでつくりました。\nhttps://yurameki.tokyo/"});setStatus("作品を共有先へ渡しました")}catch(err){if(err instanceof DOMException&&err.name==="AbortError")setStatus("共有をキャンセルしました");else{console.error(err);setStatus("共有できませんでした。保存してからお試しください")}}};
 const chooseEffect=(kind:MotionKind)=>{if(kind===effectMode)return;modeStatesRef.current[effectMode]={points:settledPoints(pointsRef.current),selected,draft:draftRef.current.map(value=>({...value})),draftCurve:draftCurveRef.current,motionSpeed:speedRef.current,rippleInterval:rippleIntervalRef.current};const restored=modeStatesRef.current[kind]??initialModeState(kind),restoredPoints=settledPoints(restored.points);pointsRef.current=restoredPoints;setPoints(restoredPoints);setSelected(restored.selected);const restoredDraft=restored.draft.map(value=>({...value}));draftRef.current=restoredDraft;setDraft(restoredDraft);draftCurveRef.current=restored.draftCurve;setDraftCurve(restored.draftCurve);speedRef.current=restored.motionSpeed;setMotionSpeed(restored.motionSpeed);rippleIntervalRef.current=restored.rippleInterval;setRippleInterval(restored.rippleInterval);setEffectMode(kind);discardReady();setShowAdvanced(false);historyRef.current=[];setHistorySize(0);setStatus(restoredPoints.length?`${kind==="flutter"?"たなびき":kind==="light"?"灯り":kind==="ripple"?"波紋":"呼吸"}の編集内容を復元しました`:kind==="flutter"?"たなびきに切り替えました。風を渡す範囲を追加してください":kind==="light"?"灯りに切り替えました。「新しい灯り」を押してください":kind==="ripple"?"波紋に切り替えました。「新しい波紋」を押してください":"呼吸に切り替えました。息づく範囲を追加してください")};
 return <main onDragEnter={dragEnter} onDragOver={event=>event.preventDefault()} onDragLeave={dragLeave} onDrop={drop}>{isDragging&&<div className="dropOverlay"><div><b>ここに画像をドロップ</b><span>JPG・PNG・WebP　／　画像は外部へ送信されません</span></div></div>}<header><div className="brand"><span className="brandName">YURAMEKI</span><small>A QUIET MOTION STUDIO</small></div><div className="privacy"><span/>作品は、この端末の中だけで息づきます</div></header><section className="intro"><h1>一枚の絵に、<em>息を宿す。</em></h1><p>風、衣、髪、光の気配。息づく場所を静かに囲み、止まっていた一瞬へ時間を結びます。</p></section><section className="studio" data-mobile-panel={mobilePanel} data-effect={effectMode}><aside className="panel left"><div className="step"><b>一</b><span>画像を置く</span></div><label className="upload"><input type="file" accept="image/*" onChange={upload}/><strong>{hasCustom?"画像を変更":isCleared?"画像を追加":"画像を選ぶ"}</strong><span>写真・イラスト｜この端末内だけで処理</span></label><div className="localOnlyNotice" role="note"><i aria-hidden="true"/><div><b>画像はアップロードされません</b><p>読み込み・加工・書き出しは、このブラウザ内だけで行われます。</p><a href="/privacy#images">画像の取扱い</a></div></div><div className="rightsNotice"><b>作品と権利を大切に</b><p>ご自身が権利を持つ画像、または加工・利用の許可を得た画像をお使いください。</p><div><a href="/terms#rights">画像利用の注意</a><a href="/privacy#images">画像の取扱い</a></div></div><div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:6,marginTop:10}}><button disabled={!hasCustom} style={{padding:8,background:fitMode==="contain"?"#28342f":"white",color:fitMode==="contain"?"white":"#28342f"}} onClick={()=>changeFit("contain")}>全体を表示</button><button disabled={!hasCustom} style={{padding:8,background:fitMode==="cover"?"#28342f":"white",color:fitMode==="cover"?"white":"#28342f"}} onClick={()=>changeFit("cover")}>余白なし</button></div><button className="ghost" onClick={restoreDemo}>デモ画像に戻す</button><button className="ghost clear" onClick={()=>{originalImageRef.current=null;const blank=document.createElement("canvas");blank.width=W;blank.height=H;sourceRef.current=blank;pointsRef.current=[];setPoints([]);setSelected(null);setDraft([]);draftRef.current=[];setHasCustom(false);setIsCleared(true);historyRef.current=[];setHistorySize(0);setStatus("画像とポイントをクリアしました")}}>画像をクリア</button><div className="tip"><b>使い方</b><p>{effectMode==="light"?"「新しい灯り」を押して場所をタップ。中心点で移動し、外側のつまみで形と向きを整えます。":effectMode==="ripple"?"「新しい波紋」を押して中心をタップ。数字で中心を、外側のつまみで届く範囲を整えます。":"画像をドロップまたは貼り付け。「新しい範囲」を押し、動きを宿したい場所を点で囲みます。"}</p></div></aside><div className="canvasWrap"><div className="canvasToolbar"><div className="canvasModes" role="group" aria-label="動きの種類">{([["呼吸","KOKYU","breath"],["波紋","HAMON","ripple"],["灯り","AKARI","light"],["たなびき","TANABIKI","flutter"]] as const).map(([name,latin,kind])=><button type="button" key={kind} data-mode={kind} aria-pressed={effectMode===kind} onClick={()=>chooseEffect(kind)}><span>{name}</span><small>{latin}</small></button>)}</div><button type="button" className="canvasGuideToggle canvasGuideMobile" onClick={()=>setShowGuides(value=>!value)} aria-pressed={!showGuides} aria-label={showGuides?"画像だけを見る":"編集ガイドを戻す"}><span aria-hidden="true">{showGuides?"◉":"○"}</span>{showGuides?"画像だけ見る":"編集ガイドを戻す"}</button></div><div className="canvasTop"><span>{isCleared?"NO IMAGE":hasCustom?"YOUR IMAGE":"YURAMEKI DEMO"}</span><button onClick={()=>setAuto(v=>!v)} className={auto?"live":""}><i/> {auto?"自動プレビュー中":"プレビュー停止中"}</button></div><canvas ref={canvasRef} width={W} height={H} aria-label="画像アニメーション編集キャンバス" onPointerDown={pointerDown} onPointerMove={pointerMove} onPointerUp={event=>{dragRef.current=null;event.currentTarget.style.cursor="grab"}} onPointerCancel={event=>{dragRef.current=null;event.currentTarget.style.cursor="crosshair"}}/>{chosen&&!hasAdjustedPoint&&showGuides&&<div className="pointCoach"><i/>{effectMode==="light"?"中心で移動、外側のつまみで形と向きを整える":effectMode==="ripple"?"数字で中心を移動、外側のつまみで届く範囲を整える":"数字で移動し、白い点で形を整える"}</div>}<div className="canvasHint">{effectMode==="light"?"中心点で移動 ・ 外側のつまみで楕円と向き":effectMode==="ripple"?"数字で中心を移動 ・ 外側のつまみで届く範囲":"数字で移動 ・ 白い点で範囲を微調整"}</div></div><nav className="mobileEditorTabs" aria-label="制作メニュー"><button type="button" aria-pressed={mobilePanel==="image"} onClick={()=>setMobilePanel("image")}><b>一</b>画像</button><button type="button" aria-pressed={mobilePanel==="motion"} onClick={()=>setMobilePanel("motion")}><b>二</b>動き</button><button type="button" onClick={()=>{chooseOutputSize(720);setSelectedFormat("mp4");setStatus("SNSで共有しやすい720 MP4を選びました。生成するを押してください");document.querySelector(".export")?.scrollIntoView({behavior:"smooth",block:"start"})}}><b>三</b>保存</button></nav><aside className={`panel right${showAdvanced?" advanced-open":""}`}><div className="step"><b>二</b><span>{effectMode==="flutter"?"風を渡す場所":effectMode==="light"?"灯す場所":effectMode==="ripple"?"波紋を広げる場所":"息づく場所"}</span></div><button type="button" className="canvasGuideToggle canvasGuideDesktop" onClick={()=>setShowGuides(value=>!value)} aria-pressed={!showGuides} aria-label={showGuides?"画像だけを見る":"編集ガイドを戻す"}><span aria-hidden="true">{showGuides?"◉":"○"}</span>{showGuides?"画像だけ見る":"編集ガイドを戻す"}</button><div style={{display:"grid",gap:8,marginBottom:20}}><button style={{padding:10,border:"1px solid #28342f",background:"#28342f",color:"white",cursor:"pointer"}} onClick={startRegion}>{effectMode==="light"?"＋ 新しい灯り":effectMode==="ripple"?"＋ 新しい波紋":"＋ 新しい範囲"}</button>{selected===null&&effectMode!=="light"&&effectMode!=="ripple"&&<><p className="rangeDraftStatus" aria-live="polite">{draft.length<3?`あと${3-draft.length}点で範囲を作れます`:"囲んだ形を確認して決定してください"}</p><div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:6}}><button style={{padding:8,background:draftCurve?"#28342f":"white",color:draftCurve?"white":"#28342f"}} onClick={()=>{setDraftCurve(true);draftCurveRef.current=true}}>曲線</button><button style={{padding:8,background:!draftCurve?"#28342f":"white",color:!draftCurve?"white":"#28342f"}} onClick={()=>{setDraftCurve(false);draftCurveRef.current=false}}>直線</button></div><button style={{padding:8,cursor:"pointer"}} disabled={!draft.length} onClick={undoDraft}>ひとつ前の点に戻す</button><button type="button" className="finishRegionButton" disabled={draft.length<3} onClick={finishRegion}>{draft.length<3?`あと${3-draft.length}点選んでください`:"✓ この範囲で決定"}</button><button style={{padding:8,border:0,background:"transparent",textDecoration:"underline",cursor:"pointer"}} onClick={cancelRegion}>キャンセル</button></>}</div>{chosen?<><div className="pointHead"><span><i/> {effectMode==="light"?"LIGHT":effectMode==="ripple"?"RIPPLE":"RANGE"} {points.findIndex(p=>p.id===selected)+1}</span><div><button disabled={!historySize} onClick={undo}>元に戻す</button><button onClick={()=>{saveHistory();const n=points.filter(p=>p.id!==selected);pointsRef.current=n;setPoints(n);setSelected(n[0]?.id??null)}}>削除</button></div></div>{effectMode==="light"&&<div className="syncControl" role="group" aria-label="灯りの形"><span>光のかたち</span><div><button className={Math.abs((chosen.lightAspect??1)-1)<.06?"active":""} onClick={()=>{saveHistory();updateChosen({lightAspect:1});setStatus("灯りを円に戻しました")}}>円</button><button className={Math.abs((chosen.lightAspect??1)-1)>=.06?"active":""} onClick={()=>{saveHistory();updateChosen({lightAspect:Math.abs((chosen.lightAspect??1)-1)<.06?1.55:chosen.lightAspect,lightAngle:chosen.lightAngle??0});setStatus("外側のつまみを動かして、楕円と向きを整えられます")}}>楕円</button></div></div>}{effectMode==="ripple"?(points.length>1?<div className="syncControl" role="group" aria-label="波紋のタイミング"><span>波紋のタイミング</span><div><button className={chosen.synced!==false?"active":""} onClick={()=>updateChosen({synced:true,phaseOffset:0})}>波を合わせる</button><button className={chosen.synced===false?"active":""} onClick={()=>updateChosen({synced:false,phaseOffset:chosen.phaseOffset||Math.PI*.7})}>順に広がる</button></div></div>:null):<div className="syncControl" role="group" aria-label={effectMode==="light"?"灯りのタイミング":"範囲ごとの動きのタイミング"}><span>{effectMode==="flutter"?"風のタイミング":effectMode==="light"?"灯りのタイミング":"呼吸のタイミング"}</span><div><button className={chosen.synced!==false?"active":""} onClick={()=>updateChosen({synced:true,phaseOffset:0})}>{effectMode==="flutter"?"風を合わせる":effectMode==="light"?"灯りを合わせる":"息を合わせる"}</button><button className={chosen.synced===false?"active":""} onClick={()=>updateChosen({synced:false,speed:chosen.synced===false?(chosen.speed??motionSpeed):motionSpeed,phaseOffset:chosen.phaseOffset||Math.PI*.7})}>{effectMode==="flutter"?"それぞれにたなびく":effectMode==="light"?"それぞれに灯る":"それぞれに呼吸"}</button></div></div>}<label className="range"><span>{effectMode==="flutter"?"風の速さ":effectMode==="light"?"またたきの速さ":effectMode==="ripple"?"広がる速さ":"息の速さ"}（スピード）<output>{chosenSpeed}</output></span><input type="range" min={0} max={200} value={chosenSpeed} onPointerDown={()=>{if(chosen.synced===false)saveHistory()}} onChange={e=>changeChosenSpeed(+e.target.value)}/></label>{effectMode==="ripple"&&<label className="range"><span>波の間（発生間隔）<output>{rippleInterval.toFixed(1)}秒</output></span><input type="range" min={.4} max={8} step={.1} value={rippleInterval} onChange={e=>changeRippleInterval(+e.target.value)}/></label>}{(effectMode==="light"?([["amplitude","灯りの強さ",0,60],["radius","光の広がり",40,240],["stiffness","ぬくもり",5,90],["damping","またたき",30,98],["whole","周囲への照り返し",0,60],["feather","なじみ",2,60]] as const):effectMode==="ripple"?([["amplitude","波紋の強さ",0,12],["impact","雫の気配",0,100],["radius","届く範囲",40,320],["stiffness","輪の間隔",16,90],["feather","なじみ",4,80]] as const):([[ "amplitude",effectMode==="flutter"?"風の強さ（振幅）":"息の深さ（振幅）",0,60],["whole",effectMode==="flutter"?"風のうねり（全体揺れ）":"漂い（全体揺れ）",0,60],["stiffness","張り（硬さ）",5,90],["damping","名残（余韻）",30,98],["outer","伝わり（外側への影響）",0,120],["feather","なじみ（境界ぼかし）",2,60]] as const)).map(([key,label,min,max])=><label className={`range${key==="amplitude"?"":" advancedRange"}`} key={key}><span>{label}<output>{key==="amplitude"&&(effectMode==="breath"||effectMode==="ripple")?pointValue(key).toFixed(1).replace(".0",""):Math.round(pointValue(key))}</output></span><input type="range" step={key==="amplitude"&&effectMode==="ripple"?.1:1} min={min} max={key==="amplitude"&&effectMode==="breath"?100:max} value={key==="amplitude"&&effectMode==="breath"?breathDepthToSlider(pointValue(key)):pointValue(key)} onPointerDown={saveHistory} onChange={e=>update(key,key==="amplitude"&&effectMode==="breath"?sliderToBreathDepth(+e.target.value):+e.target.value)}/></label>)}<button type="button" className="mobileAdvancedToggle" aria-expanded={showAdvanced} onClick={()=>setShowAdvanced(value=>!value)}>{showAdvanced?"細かい調整を閉じる":"細かく整える"}</button></>:<div className="empty">{effectMode==="light"?<>「新しい灯り」を押し<br/>灯したい場所をタップしてください</>:effectMode==="ripple"?<>「新しい波紋」を押し<br/>中心をタップしてください</>:<>画像上を順番にクリックして<br/>揺らす範囲を囲んでください</>}</div>}<div className="points"><span>{effectMode==="light"?"灯り":effectMode==="ripple"?"波紋":"範囲"}</span><div className="pointRail"><button type="button" className={`pointRailArrow${pointRailState.overflow?" visible":""}`} disabled={!pointRailState.left} onClick={()=>scrollPointRail(-1)} aria-label="前の範囲を表示">‹</button><div className="pointRailTrack" ref={pointRailRef} onScroll={updatePointRail}>{points.map((p,i)=><button type="button" key={p.id} data-point-id={p.id} aria-label={`${effectMode==="light"?"灯り":effectMode==="ripple"?"波紋":"範囲"}${i+1}を選択`} aria-pressed={p.id===selected} className={p.id===selected?"active":""} onClick={()=>setSelected(p.id)}>{i+1}</button>)}</div><button type="button" className={`pointRailArrow${pointRailState.overflow?" visible":""}`} disabled={!pointRailState.right} onClick={()=>scrollPointRail(1)} aria-label="次の範囲を表示">›</button></div></div></aside></section><section className="export"><div><p className="eyebrow">三　作品を結ぶ</p><h2>動きを持ち帰る</h2><div className={ready?"completion ready":"completion"}><p aria-live="polite">{ready?(effectMode==="ripple"?"波紋が、宿りました。":"息が、宿りました。"):status}</p>{ready&&<small>{status}</small>}{(exporting||ready)&&<div className="progressTrack" aria-label={`生成進捗 ${exportProgress}%`}><i style={{width:`${exportProgress}%`}}/></div>}</div></div><div className="exportBtns"><button disabled={exporting} aria-pressed={outputSize===512} style={outputSize===512?{background:"#28342f",color:"white"}:undefined} onClick={()=>chooseOutputSize(512)}><b>512</b><span>低負荷モード</span></button><button disabled={exporting} aria-pressed={outputSize===720} style={outputSize===720?{background:"#28342f",color:"white"}:undefined} onClick={()=>chooseOutputSize(720)}><b>720</b><span>標準画質</span></button><button disabled={exporting} aria-pressed={outputSize===1080} style={outputSize===1080?{background:"#28342f",color:"white"}:undefined} onClick={()=>chooseOutputSize(1080)}><b>1080</b><span>高画質・元画像を活用</span></button><button disabled={exporting} aria-pressed={selectedFormat==="gif"} style={selectedFormat==="gif"?{background:"#28342f",color:"white"}:undefined} onClick={()=>chooseExportFormat("gif")}><b>GIF</b><span>24フレーム・ループ</span></button><button disabled={exporting} aria-pressed={selectedFormat==="apng"} style={selectedFormat==="apng"?{background:"#28342f",color:"white"}:undefined} onClick={()=>chooseExportFormat("apng")}><b>APNG</b><span>高画質・ループ</span></button><button disabled={exporting} aria-pressed={selectedFormat==="webp"} style={selectedFormat==="webp"?{background:"#28342f",color:"white"}:undefined} onClick={()=>chooseExportFormat("webp")}><b>WebP</b><span>軽量・ループ</span></button><button disabled={exporting} aria-pressed={selectedFormat==="mp4"} style={selectedFormat==="mp4"?{background:"#28342f",color:"white"}:undefined} onClick={()=>chooseExportFormat("mp4")}><b>MP4</b><span>SNS向け・最低5秒</span></button><button disabled={!selectedFormat||exporting} onClick={()=>selectedFormat&&exportFile(selectedFormat)}><b>{exporting?"生成中…":"生成する"}</b><span>{selectedFormat?(selectedFormat==="mp4"?"SNS向け・最低5秒":`${selectedFormat.toUpperCase()}・24フレーム`):"形式を選択"}</span></button>{ready&&<><button className="shareReady" onClick={shareReadyFile}><b>共有する</b><span>この端末の共有先へ</span></button><button className="saveReady" onClick={saveReadyFile}><b>保存する</b><span>{ready.name}</span></button></>}</div></section><aside className="supportStrip"><div><span>SUPPORT YURAMEKI</span><h2>静かな道具を、育てていく。</h2><p>YURAMEKIが制作の余白であり続けられるよう、Maruti Labの開発を支援できます。</p></div>{supportUrl?<a href={supportUrl} target="_blank" rel="noreferrer"><span className="coffeeMark" aria-hidden="true">☕︎</span><span className="supportLinkCopy"><b>Buy Me a Coffee</b><small>外部の支援ページへ</small></span></a>:<span className="supportPending">SUPPORT PAGE<br/>COMING SOON</span>}</aside><section className="landscapes"><div><p className="eyebrow">NEXT BREATHSCAPES</p><h2>これからの景色</h2><p>静止画の中にある、まだ見えない気配。</p></div><div className="landscapeGrid" aria-label="準備中の動き">{([["≋","ゆらぎ","YURAGI","水面のように移ろう"],["✦","きらめき","KIRAMEKI","光をひと粒添える"],["◌","霞","KASUMI","淡く流れてほどける"],["⌇","陽炎","KAGERO","熱の向こうで揺れる"]] as const).map(([icon,name,latin,copy])=><button type="button" disabled className="pending" key={name}><span aria-hidden="true">{icon}</span><b>{name}<em>{latin}</em></b><small>{copy}</small><i aria-hidden="true">準備中</i></button>)}</div></section><footer><span>YURAMEKI — A MARUTI LAB PROJECT</span><nav aria-label="サイト情報"><a href="/yurameki/faq">FAQ</a><a href="/terms">利用規約</a><a href="/privacy">プライバシー</a><a href="/yurameki/credits">Credits</a><a href="/contact">お問い合わせ</a></nav><span>制作画像はブラウザ内で処理されます</span></footer></main>}
